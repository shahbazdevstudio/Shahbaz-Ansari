import Silk from '../react-bits/Silk';

const Background = () => {
  return (
    <Silk
      speed={5}
      scale={1}
      color="#2e2e2e"
      noiseIntensity={1.5}
      rotation={0}
    />
  );
}

export default Background
